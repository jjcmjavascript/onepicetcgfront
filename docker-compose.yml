version: '3.9'
services:
  node:
    build:
      context: ./
      dockerfile: dockerfile
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - 3000:3000
    networks:
      - default
    env_file: .env
